<page xmlns="http://jboss.com/products/seam/pages" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://jboss.com/products/seam/pages http://jboss.com/products/seam/pages-2.1.xsd">

	<param name="edit" />
	<param name="objectId" />
	<begin-conversation join="true" />
	<action execute="#{syntheseClientBean.init}" />
	<restrict>#{persistentPermissionResolver.hasPermission('customerSummary','customerSummaryVisualization') or persistentPermissionResolver.hasPermission('account','accountManagement') or (!edit and persistentPermissionResolver.hasPermission('account','accountVisualization'))}</restrict>

</page>